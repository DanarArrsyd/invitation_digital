const {chromium}=require('playwright');const fs=require('fs');const assert=require('assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const results=[];
const p=await b.newPage({viewport:{width:390,height:844},reducedMotion:'no-preference'});const errors=[];p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});let calls=0;
await p.route('**/ni-visual-review*',async r=>{if(r.request().method()==='POST'){calls++;return r.fulfill({status:200,contentType:'text/x-component',body:'0:{"a":"$@1","f":"","b":"development"}\n1:{"status":"success"}\n'})}return r.continue()});
await p.goto('http://localhost:3000/ni-visual-review');assert.equal(await p.getByText('Tamu Pengujian',{exact:true}).count(),1);await p.keyboard.press('Tab');assert.equal(await p.evaluate(()=>document.activeElement.textContent.trim()),'Buka Undangan');await p.keyboard.press('Enter');await p.waitForTimeout(1100);assert.equal(await p.getByRole('button',{name:'Buka Undangan'}).count(),0);assert.equal(await p.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Isi undangan');assert.equal(calls,1);results.push('Cover keyboard opening, focus transfer, guest greeting, one cover action: PASS');
assert.equal(await p.locator('audio').evaluate(e=>e.paused),false);await p.getByRole('button',{name:'Jeda musik'}).click();assert.equal(await p.locator('audio').evaluate(e=>e.paused),true);await p.getByRole('button',{name:'Putar musik'}).click();assert.equal(await p.locator('audio').evaluate(e=>e.paused),false);results.push('Music starts after opening; pause and resume: PASS (synthetic local WAV)');
await p.getByRole('button',{name:'Kirim Konfirmasi'}).scrollIntoViewIfNeeded();assert.equal(await p.getByRole('button',{name:'Kirim Konfirmasi'}).isDisabled(),true);await p.getByRole('button',{name:'Hadir',exact:true}).click();assert.equal(await p.locator('input[name="attendance"]').inputValue(),'attending');await p.getByRole('button',{name:'Tidak Hadir',exact:true}).click();assert.equal(await p.locator('input[name="attendance"]').inputValue(),'not_attending');await p.getByRole('button',{name:'Kirim Konfirmasi'}).click();await p.getByText('Konfirmasi kehadiran Anda telah kami terima.').waitFor();results.push('RSVP choices, disabled guard, success presentation: PASS (mocked action response)');
await p.locator('textarea[name="message"]').fill('Ucapan QA');await p.getByRole('button',{name:'Kirim Ucapan',exact:true}).click();await p.getByText('Terima kasih atas ucapan dan doanya.').waitFor();assert.equal(await p.getByText('Tamu Uji 7',{exact:true}).count(),0);await p.getByRole('button',{name:'Muat Lebih Banyak'}).click();assert.equal(await p.getByText('Tamu Uji 7',{exact:true}).count(),1);results.push('Wishes success presentation and pagination: PASS (mocked action response; fixture list)');
for(let y=0;y<await p.locator('body').evaluate(e=>e.scrollHeight);y+=650){await p.evaluate(y=>window.scrollTo(0,y),y);await p.waitForTimeout(90)}await p.waitForTimeout(1200);assert.equal(calls,3);const invisible=await p.locator('h1,h2,h3').evaluateAll(es=>es.filter(e=>{let x=e;while(x){if(getComputedStyle(x).opacity==='0')return true;x=x.parentElement}return false}).map(e=>e.textContent));assert.equal(invisible.length,0);results.push('Normal-motion scroll reveals all headings; no duplicate actions on scroll: PASS');
await p.close();
for(const [feature,selector] of [['countdown','text=Menghitung Hari'],['story','h2:has-text("Perjalanan Kami")'],['gallery','h2:has-text("Galeri")'],['rsvp','h2:has-text("RSVP")'],['wishes','h2:has-text("Doa & Ucapan")'],['gift','h2:has-text("Wedding Gift")'],['livestream','h2:has-text("Live Streaming")'],['maps','a:has-text("Lihat Lokasi")'],['music','audio'],['guestPersonalization','text=Tamu Pengujian']]){
 const t=await b.newPage({reducedMotion:'reduce'});await t.route('**/*',async r=>r.request().method()==='POST'?r.fulfill({status:200,contentType:'text/x-component',body:'0:{"a":"$@1","f":"","b":"development"}\n1:null\n'}):r.continue());await t.goto('http://localhost:3000/ni-visual-review?off='+feature);await t.getByRole('button',{name:'Buka Undangan'}).click();await t.waitForTimeout(100);assert.equal(await t.locator(selector).count(),0,feature);results.push(feature+' OFF: PASS');await t.close();
}
// Actual server validation, with a non-existing invitation ID and no Turnstile token.
const t=await b.newPage({viewport:{width:320,height:740},reducedMotion:'reduce'});await t.goto('http://localhost:3000/ni-visual-review?anonymous=1');await t.getByRole('button',{name:'Buka Undangan'}).click();const forms=t.locator('form');await forms.nth(0).locator('input[name="guestName"]').fill('QA');await t.getByRole('button',{name:'Hadir',exact:true}).click();await t.getByRole('button',{name:'Kirim Konfirmasi'}).click();await forms.nth(0).getByRole('alert').waitFor();results.push('RSVP real server rejects missing verification: '+await forms.nth(0).getByRole('alert').innerText());await forms.nth(1).locator('input[name="guestName"]').fill('QA');await t.locator('textarea').fill('QA test');await t.getByRole('button',{name:'Kirim Ucapan',exact:true}).click();await forms.nth(1).getByRole('alert').waitFor();results.push('Wishes real server rejects missing verification: '+await forms.nth(1).getByRole('alert').innerText());await t.close();
fs.writeFileSync('/tmp/ni-redesign/interactions.json',JSON.stringify({results,errors},null,2));console.log({results,errors});await b.close();})();
