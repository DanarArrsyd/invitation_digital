const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const ts=require(process.cwd()+'/node_modules/typescript');
const source=fs.readFileSync('src/server/public/invitation-loader.ts','utf8');const js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText;
(async()=>{const results=[];for(const [label,row,token,expected] of [
['missing',null,undefined,'not_found'],['draft',{status:'draft'},undefined,'not_found'],['archived',{status:'archived'},undefined,'not_found'],['expired status',{status:'expired'},undefined,'expired'],['expired date',{status:'published',expires_at:'2000-01-01'},undefined,'expired'],['published',{status:'published'},undefined,'ok'],['valid guest',{status:'published'},'valid','ok'],['invalid guest',{status:'published'},'invalid','ok']]){
 const inv=row?{id:'qa',title:'QA',event_date:'2026-10-20',theme:{slug:'nusantara-ivory'},expires_at:'2100-01-01',...row}:null;
 const client={from(table){let filter={};const q={select(){return q},eq(k,v){filter[k]=v;return q},maybeSingle(){return Promise.resolve({data:table==='invitations'?inv:filter.token==='valid'?{id:'guest',display_name:'Guest QA',token:'valid',notes:null}:null,error:null})},then(resolve){return Promise.resolve({data:[{full_name:'QA'}]}).then(resolve)}};return q}};
 const exports={};vm.runInNewContext(js,{exports,require(id){if(id.includes('supabase/admin'))return {createSupabaseAdminClient:()=>client};if(id.includes('coupleName'))return {getCoupleDisplayName:()=> 'QA'};if(id.includes('normalize'))return {loadNormalizedInvitation:async()=>({id:'qa'})};throw Error(id)},Date});
 const result=await exports.getPublicInvitationBySlug('qa',token);assert.equal(result.kind,expected);if(label==='valid guest')assert.equal(result.guest.displayName,'Guest QA');if(label==='invalid guest')assert.equal(result.guest,null);results.push(label+': PASS');
}console.log(results);fs.writeFileSync('/tmp/ni-redesign/loader-results.json',JSON.stringify({method:'Actual loader source executed with mocked Supabase reads and normalized data; no database writes.',results},null,2))})();
